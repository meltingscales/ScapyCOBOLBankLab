[project]
name = "scapy-cobol-bank-lab"
version = "0.1.0"
description = "Scapy command injection lab with vulnerable mainframe simulator"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "scapy>=2.6.0",
]

[dependency-groups]
dev = []
